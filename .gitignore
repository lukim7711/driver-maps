# Dependency directories
node_modules/
**/node_modules/

# Build outputs
dist/
**/dist/
build/
.firebase/
firebase-debug.log

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
server.log

# Environment variables & secrets
.env
.env.*
**/sa-key.json
sa-key.json
*.pem
*.key

# OS files
.DS_Store
Thumbs.db

# Other
backend.tar.gz

# Wrangler local cache (jangan commit)
.wrangler/
.wrangler/**/*

# OpenCode personal config (contains API keys)
opencode.json
.opencode/agents/
.opencode/.gitignore

# Arsip file lama (tidak perlu di-push)
arsip/

# Test images (tidak perlu di-push)
gambar/

# AGENTS.md pribadi (tidak perlu di-push)
AGENTS.md

# OpenCode config example (tidak perlu di-push)
global-opencode-config-example.json
